version: '3.4'
services:
    back:
        build: ./back
        command: /go/src/ant-farm/main
        ports:
            - '80:8081'
    front:
        build: ./front
        command: npm run build
        ports:
            - '3500:3000'
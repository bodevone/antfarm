version: '3.4'
services:
    back:
        build: ./back
        command: /go/src/ant-farm/main
        ports:
            - '8080:8081'
    front:
        build: ./front
        command: node server.js
        ports:
            - '3500:3000'